#include <stdio.h>
#include <stdlib.h>

int main(int argc, const char * argv[])
{
    FILE *fp; // declarando ponteiro para arquivo
    fp = fopen("ola_mundo.txt","w"); // abrindo o arquivo para escreita

    char nome[] =  "ola mundo"; // string constante 

    fputs(nome, fp);
    putc('\n', fp);

    return 0;

}
